# Copy to training/.env — used by the publish stage and backend/scripts/*_pretrained.py.
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# Optional: after publishing, ask the running engine to reload models immediately.
ENGINE_URL=
ENGINE_SHARED_SECRET=
